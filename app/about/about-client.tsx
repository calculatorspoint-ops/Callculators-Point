/**
 * app/about/about-client.tsx — DEPRECATED
 *
 * The About page now imports the About view directly in page.tsx
 * for server-side rendering (SSR). This file is no longer used.
 *
 * It is retained temporarily to avoid breaking any stale imports.
 * Safe to delete once you confirm there are no other consumers.
 */
export default function AboutClient() { return null; }
